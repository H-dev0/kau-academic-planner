[project]
name = "kau-program-scraper"
version = "0.1.0"
description = "Local-first pilot for extracting and validating official university program data."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

